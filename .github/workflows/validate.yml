name: validate
on:
  push:
  pull_request:
jobs:
  verify-patches:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/verify-patches.mjs
